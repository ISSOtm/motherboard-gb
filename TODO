Fix SGB border (too many tiles)
Fix color fading (especially bad on SGB)
Fix color layout on language menu (perhaps also on main menu)
* Transition out of title screen
Make a more consistent system for facing directions
Store less NPC data in RAM
Make player interact with what's in front of them instead of at their feet
Language features (different strings)
Main menu (finish it)
Dynamic collision detection (source some collision mappings from RAM and modify them)
Pause menu
Automatic text centering (problem: what if the line is cut in the middle by a buffer refill?)
Finish overlap fix
Compress title screen XOR masks and unpack 'em to WRAM (should be done late, since it might crush uninitialized memory exceptions, and isn't very invasive)
